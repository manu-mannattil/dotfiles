#!/bin/sh

# If PCManFM is running, then upon opening a new tab (as
# opposed to a new window), i3 doesn't give the window
# focus.  So switch to workspace 1 first.
if { pgrep -x i3 && pgrep -x pcmanfm; } >/dev/null 2>&1
then
    i3-msg workspace 1
fi

exec /usr/bin/pcmanfm --no-desktop "$@"
